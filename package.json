{
  "name": "kamsis-secure-auth",
  "version": "1.0.0",
  "description": "Dev tooling for the PHP-based secure auth container app.",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -i ./resources/tailwind.css -o ./public/styles.css --minify",
    "commitlint": "commitlint --config commitlint.config.cjs"
  },
  "keywords": [
    "commitlint"
  ],
  "author": "Codex",
  "license": "ISC",
  "devDependencies": {
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "tailwindcss": "^3.4.17"
  }
}
